name: Mobile Release Validate

on:
  push:
    tags:
      - "v-validate-*"

env:
  TEST_SECRET: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print secret presence
        run: echo "SECRET=${TEST_SECRET:-<missing>}"
