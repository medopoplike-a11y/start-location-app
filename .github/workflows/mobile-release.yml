name: Mobile Release + OTA

on:
  push:
    tags:
      - "v*"

jobs:
  release-mobile:
    runs-on: ubuntu-latest

    steps:
      - name: Confirm workflow trigger
        run: echo "Mobile release workflow triggered on push tag"
