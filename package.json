{
  "name": "start-new",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "dev": "next dev --hostname localhost --port 3000",
    "build": "next build",
    "build:mobile": "node scripts/build-static-export.js && node fix-paths.js && node prepare-android.js && npx cap sync android",
    "build:android-release": "npm run build:mobile && node -e \"const { execSync } = require('child_process'); const cmd = process.platform === 'win32' ? 'cd android && gradlew.bat assembleRelease' : 'cd android && ./gradlew assembleRelease'; execSync(cmd, { stdio: 'inherit' });\"",
    "build:android-release:win": "npm run build:mobile && node scripts/android-ensure-sdk.js && cd android && gradlew.bat assembleRelease",
    "build:android-debug:win": "npm run build:mobile && node scripts/android-ensure-sdk.js && cd android && gradlew.bat assembleDebug",
    "build:win": "node scripts/build-static-export.js && node fix-paths.js && node prepare-android.js && npx cap sync android",
    "build:ota": "node scripts/create-ota-bundle.js",
    "prepare-android": "node prepare-android.js",
    "start": "next start",
    "lint": "eslint . --max-warnings=200",
    "postinstall": "node fix-gradle-proguard.js",
    "bundle:ota": "npm run build:mobile && npm run build:ota"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.26",
    "@capacitor/android": "^8.2.0",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/cli": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/geolocation": "^8.0.0",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/push-notifications": "^8.0.2",
    "@capacitor/toast": "^8.0.1",
    "@capgo/capacitor-updater": "^8.43.11",
    "@supabase/supabase-js": "^2.101.1",
    "@types/leaflet": "^1.9.21",
    "framer-motion": "^12.38.0",
    "leaflet": "^1.9.4",
    "lucide-react": "^1.0.1",
    "next": "16.2.1",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-leaflet": "^5.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "19.2.14",
    "@types/react-dom": "^19",
    "archiver": "^5.3.1",
    "cross-env": "^7.0.3",
    "eslint": "^9.16.0",
    "eslint-config-next": "16.2.1",
    "tailwindcss": "^4",
    "typescript": "5.9.3"
  }
}
